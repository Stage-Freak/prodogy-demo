name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      DEPLOY_TOKEN: sk-live-xyz789
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@main
      - run: npm install && npm test
